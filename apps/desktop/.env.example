# Desktop App Configuration (Electron Mode)

# Clean start mode - clears all data on startup
# CLEAN_START=1

# E2E Testing - skips onboarding
# E2E_SKIP_AUTH=1

# Browser Mode Configuration
# Uncomment these when running the renderer in a browser instead of Electron

# Backend API URL (web-server)
# VITE_API_BASE_URL=http://localhost:3001

# WebSocket URL for real-time updates
# VITE_WS_URL=ws://localhost:3002
