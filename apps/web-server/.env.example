# Web Server Configuration

# Server ports
PORT=3001
WS_PORT=3002

# Data directory (for SQLite database)
DATA_DIR=./data

# ⚠️ REQUIRED: Encryption key for API keys
# Generate a secure random string (32+ characters) for production!
# Example: openssl rand -base64 32
# DO NOT use the example value below - generate your own!
ENCRYPTION_KEY=CHANGE_ME_TO_A_SECURE_RANDOM_STRING_32_PLUS_CHARS

# Allowed origins for CORS (comma-separated)
# For production, set this to your actual domain
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Node environment
NODE_ENV=development
